/**
 * Insights are derived from match/standings data (top scorers, recent form,
 * simple win-likelihood per fixture). Starts as a placeholder; we wire real
 * data once the basic flow is validated end-to-end.
 */
export default function InsightsPage() {
  return (
    <section className="space-y-3">
      <h2 className="text-lg font-semibold">Insights</h2>
      <p className="text-slate-600">
        Em breve: artilheiros, forma recente das seleções e probabilidade simples por confronto.
      </p>
    </section>
  );
}
